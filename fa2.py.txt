def calculate_fee(dist, rate):
    fee = dist * rate
    return fee

dist = float(input("Enter distance in kilometers: "))
rate = float(input("Enter rate per kilometer (₱): "))

total_fee = calculate_fee(dist, rate)
print(f"Total Delivery Fee: ₱{total_fee:.2f}")